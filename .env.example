# Domain name for SSL certificates
DOMAIN=yourdomain.com

# Path to certbot certificates directory
CERT_PATH=../mail-infra/services/silver-config/certbot/keys/etc

# OAuth2 Configuration (Required)
# The introspection URL is used by the API server to validate application access tokens
OAUTH2_INTROSPECT_URL=https://localhost:8090/oauth2/introspect

# Notes:
# 1. OAUTH2_INTROSPECT_URL is REQUIRED
# 2. The API server validates tokens by posting to the introspection endpoint
# 3. Client applications must obtain their own tokens from the OAuth2 server
# 4. User SMTP credentials are passed in the API request body
