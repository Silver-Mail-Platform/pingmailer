# Domain name for SSL certificates
DOMAIN=yourdomain.com

# Path to certbot certificates directory
CERT_PATH=../mail-infra/services/silver-config/certbot/keys/etc

# OAuth2 Configuration (Required)
# These credentials are used by the API server to validate application access tokens
OAUTH2_TOKEN_URL=https://localhost:8090/oauth2/token
OAUTH2_CLIENT_ID=your-client-id
OAUTH2_CLIENT_SECRET=your-client-secret

# Notes:
# 1. OAUTH2_CLIENT_ID and OAUTH2_CLIENT_SECRET are REQUIRED
# 2. The API server uses these to obtain and validate access tokens
# 3. Client applications must obtain their own tokens from the OAuth2 server
# 4. User SMTP credentials are passed in the API request body
